# Overview

`ndx` is the shared product package used by `apps/ndx`.

Keep product rules, invariants, and framework-independent behavior here. App
modules own Express, Vite, process lifecycle, socket attachment, and UI
composition.

Start drill-down from [architecture.md](architecture.md): it maps each source
partition to its contract and the first symbol to read.
