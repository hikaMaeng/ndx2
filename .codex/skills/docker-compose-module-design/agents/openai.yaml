interface:
  display_name: "Docker Compose Module Design"
  short_description: "Enforce compose ownership and dist-only Dockerfiles."
  default_prompt: "Use $docker-compose-module-design to review or design this repository Docker orchestration and runtime image rules."
