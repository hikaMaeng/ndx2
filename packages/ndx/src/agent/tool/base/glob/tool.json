{
  "tool": {
    "command": "bash",
    "args": ["./index.sh", "{pattern}", "{path}", "{limit}"]
  },
  "schema": {
    "type": "function",
    "name": "glob",
    "description": "Finds files matching a glob pattern under the NDX virtual root.",
    "parameters": {
      "type": "object",
      "properties": {
        "pattern": { "type": "string", "description": "Glob pattern such as src/**/*.ts." },
        "path": { "type": "string", "description": "Directory to search in. Defaults to the project root; container absolute paths must stay under the NDX virtual root." },
        "limit": { "type": "number", "description": "Maximum number of files to return. Defaults to 100." }
      },
      "required": ["pattern"],
      "additionalProperties": false
    }
  }
}
