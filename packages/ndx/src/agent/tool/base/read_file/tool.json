{
  "tool": {
    "command": "bash",
    "args": ["./index.sh", "{path}", "{offset}", "{limit}"]
  },
  "schema": {
    "type": "function",
    "name": "read_file",
    "description": "Reads a text file under the NDX virtual root, optionally returning a line range.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": { "type": "string", "description": "Container absolute path under the NDX virtual root, or path relative to the project root." },
        "offset": { "type": "number", "description": "Zero-based line offset to start reading from." },
        "limit": { "type": "number", "description": "Maximum number of lines to return." }
      },
      "required": ["path"],
      "additionalProperties": false
    }
  }
}
