# Overview

`__DOMAIN_PACKAGE_NAME__` is the single domain package paired with
`apps/__SERVICE_NAME__`.

Keep product rules, invariants, and framework-independent service behavior here.
The app module owns Express, Vite, process lifecycle, and UI composition.
