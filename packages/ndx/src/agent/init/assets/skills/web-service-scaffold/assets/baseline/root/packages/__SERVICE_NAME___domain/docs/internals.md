# Internals

The scaffold starts with a minimal common export so build, lint, and test tasks
have a concrete package contract.

Add internals only when implementation details affect future maintenance.
