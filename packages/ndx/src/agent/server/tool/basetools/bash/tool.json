{
  "tool": {
    "command": "bash",
    "args": ["./index.sh", "{command}", "{workdir}", "{timeout_ms}", "{max_output_tokens}"]
  },
  "schema": {
    "type": "function",
    "name": "bash",
    "description": "Runs a bash command under the NDX virtual root. Use file tools for reading, searching, and editing files.",
    "parameters": {
      "type": "object",
      "properties": {
        "command": { "type": "string", "description": "Bash command to execute." },
        "workdir": { "type": "string", "description": "Working directory relative to the project root. Defaults to project root; container absolute paths must stay under the NDX virtual root." },
        "timeout_ms": { "type": "number", "description": "Timeout in milliseconds. Defaults to 60000." },
        "max_output_tokens": { "type": "number", "description": "Approximate token budget for returned output." }
      },
      "required": ["command"],
      "additionalProperties": false
    }
  }
}
