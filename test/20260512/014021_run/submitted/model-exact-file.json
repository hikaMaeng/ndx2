{
  "target": {
    "id": "model-exact-file",
    "title": "Exact model prompt file is selected",
    "test": "resolveModelInstruction must read .ndx/system/modelprompt/<model>.md when the session model exactly matches the prompt filename.",
    "steps": [
      {
        "id": "run-focused-node-test",
        "instruction": "Run yarn workspace ndx test -- --test-name-pattern \"model instruction resolves exact model names from file-backed prompt\".",
        "expected": "The focused node:test invocation exits 0 and the named subtest passes."
      }
    ],
    "passCriteria": "The exact model prompt file content is returned without falling through to another prompt."
  },
  "steps": [
    {
      "target": {
        "id": "run-focused-node-test",
        "instruction": "Run yarn workspace ndx test -- --test-name-pattern \"model instruction resolves exact model names from file-backed prompt\".",
        "expected": "The focused node:test invocation exits 0 and the named subtest passes."
      },
      "result": true,
      "descript": "Focused node:test 'model instruction resolves exact model names from file-backed prompt' exited 0.",
      "evidence": [
        "cwd=/mnt/f/dev/ndx2/packages/ndx",
        "command=yarn \"exec\" \"node\" \"--import\" \"tsx\" \"--test\" \"src/**/*.test.ts\" \"--test-name-pattern\" \"model instruction resolves exact model names from file-backed prompt\"",
        "  duration_ms: 0.713353\n  type: 'test'\n  ...\n1..20\n# tests 20\n# suites 0\n# pass 20\n# fail 0\n# cancelled 0\n# skipped 0\n# todo 0\n# duration_ms 4511.440529"
      ]
    }
  ],
  "result": {
    "result": true,
    "descript": "The focused context test 'model instruction resolves exact model names from file-backed prompt' passed.",
    "evidence": [
      "exitStatus=0",
      "resultPath=/mnt/f/dev/ndx2/test/20260512/014021_run/submitted/model-exact-file.json"
    ]
  }
}
