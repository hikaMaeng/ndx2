# Overview

`agent` is the session service. It owns the Express/Vite shell for the session web client and the server process that hosts the session socket surface.

Start drill-down from [architecture.md](architecture.md): it maps each source
partition to its responsibility and the first symbol to read.
